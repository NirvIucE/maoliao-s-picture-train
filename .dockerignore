# Docker 构建上下文排除清单（阶段 23）
# 目的：① 缩小上下文体积 ② 防止密钥与运行期数据被打进镜像

# ── 密钥：绝不进镜像（COPY backend/ 会带上 backend/.env，必须排除） ──
.env
**/.env

# ── 版本控制与个人配置 ──
.git
.gitignore
.trae
.vscode
.idea

# ── Python 依赖与缓存（Linux 镜像不可复用 Windows 下的 .venv） ──
**/__pycache__
**/*.pyc
**/*.pyo
**/.venv
**/venv
**/.pytest_cache
**/.mypy_cache
**/.ruff_cache
.coverage

# ── 前端依赖与旧产物（dist 由构建阶段重新生成） ──
**/node_modules
**/dist

# ── 运行期数据：容器内由命名卷提供 ──
**/uploads
**/e2e-uploads
**/logs

# ── 体积大且与镜像无关 ──
tests/perf
git上找到的AI图像编辑纯前端示例项目
git上找到的第二个AI项目
